#
# Not contributed yet
#
Pod::Spec.new do |s|
  s.name         = "QMBTabView"
  s.version      = "0.0.1"
  s.summary      = "A short description of QMBTabView."

  s.homepage     = "https://github.com/quemb/QMBTabs"
  s.license      = { :type => 'MIT', :file => 'LICENSE' }
  s.author       = { "Toni Moeckel" => "tonimoeckel@gmail.com" }

  s.source       = { :git => "https://github.com/quemb/QMBTabs", :tag => "0.0.1" }

  s.platform     = :ios, '5.0'

end
